import React from 'react';

const Loading = () => {
    return (
        <div>
            <div className="radial-progress text-primary">Loading</div>
        </div>
    );
};

export default Loading;